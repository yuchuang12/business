# Architecture Decision Records

公共协议、模块边界、Provider 选择、状态机语义或安全策略发生重要决策时，在本目录新增 ADR。

文件名采用 `NNNN-short-title.md`。状态至少包含：`proposed`、`accepted`、`superseded`、`rejected`。

每份 ADR 必须描述：背景、决策、备选方案、影响、迁移/兼容策略、批准人和日期。
